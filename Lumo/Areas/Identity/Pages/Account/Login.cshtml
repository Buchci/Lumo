@page
@model LoginModel

@{
    ViewData["Title"] = "Witaj ponownie";
}

<div class="container mt-5">
    <div class="row justify-content-center">
        <div class="col-md-5">
            <div class="card shadow border-0 p-4" style="border-radius: 20px; background-color: var(--card-bg);">
                <div class="text-center mb-4">
                    <div class="display-5 mb-2">🌿</div>
                    <h2 class="fw-bold" style="color: var(--main-color);">@ViewData["Title"]</h2>
                    <p class="text-muted">Zaloguj się, aby kontynuować swoją podróż.</p>
                </div>

                <form id="account" method="post">
                    <div asp-validation-summary="ModelOnly" class="text-danger mb-3" role="alert"></div>

                    <div class="form-floating mb-3">
                        <input asp-for="Input.Email" class="form-control custom-input" autocomplete="username" aria-required="true" placeholder="name@example.com" />
                        <label asp-for="Input.Email">Adres E-mail</label>
                        <span asp-validation-for="Input.Email" class="text-danger small"></span>
                    </div>

                    <div class="form-floating mb-2">
                        <input asp-for="Input.Password" class="form-control custom-input" autocomplete="current-password" aria-required="true" placeholder="password" />
                        <label asp-for="Input.Password">Hasło</label>
                        <span asp-validation-for="Input.Password" class="text-danger small"></span>
                    </div>

                    <div class="d-flex justify-content-between align-items-center mb-4 px-1">
                        <div class="form-check">
                            <input class="form-check-input" asp-for="Input.RememberMe" />
                            <label class="form-check-label text-muted small" asp-for="Input.RememberMe">
                                @Html.DisplayNameFor(m => m.Input.RememberMe)
                            </label>
                        </div>
                        <a id="forgot-password" asp-page="./ForgotPassword" class="text-decoration-none small" style="color: var(--main-color);">Zapomniałeś hasła?</a>
                    </div>

                    <button id="login-submit" type="submit" class="w-100 btn btn-lumo btn-lg py-3 mb-3">
                        Zaloguj się
                    </button>

                    <div class="text-center mt-2">
                        <p class="text-muted small">
                            Nie masz jeszcze konta?
                            <a asp-page="./Register" asp-route-returnUrl="@Model.ReturnUrl" class="text-decoration-none fw-bold" style="color: var(--main-color);">Zarejestruj się</a>
                        </p>
                        <p>
                            <a id="resend-confirmation" asp-page="./ResendEmailConfirmation" class="text-muted small text-decoration-none">Wyślij ponownie potwierdzenie e-mail</a>
                        </p>
                    </div>
                </form>
            </div>
        </div>
    </div>
</div>

<style>
    /* Spójność z Register.cshtml */
    .custom-input {
        border: 2px solid transparent;
        border-radius: 12px;
        background-color: rgba(0,0,0,0.05);
        transition: all 0.3s ease;
    }

        .custom-input:focus {
            border-color: var(--main-color);
            box-shadow: none;
            background-color: transparent;
        }

    /* Styl dla checkboxa, żeby pasował do Lumo */
    .form-check-input:checked {
        background-color: var(--main-color);
        border-color: var(--main-color);
    }

    [data-theme='dark'] .custom-input {
        background-color: rgba(255,255,255,0.05);
        color: white;
    }
</style>

@section Scripts {
    <partial name="_ValidationScriptsPartial" />
}